// write a function called same, which accepts two arrays.
// the function should return true if every value in the arra has its corresponding value squared in ther second array.
// the frequency of values must be the same.

const same = (arr1, arr2) => {};
